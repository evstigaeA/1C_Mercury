
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ЭтотОбъект.Количество() > 0 Тогда
		ТекЗапись = ЭтотОбъект.Получить(0);
		Если ТекЗапись.ДМ_ДокументыОбменаDanone.ДМ_СвязанныеДокументыУВС.Найти(ТекЗапись.ТранспортнаяОперация) = Неопределено Тогда
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Транспортная операция должна быть связана с документом обмена Danone");
			Отказ = Истина;
		КонецЕсли;	
	КонецЕсли;	
	
КонецПроцедуры
