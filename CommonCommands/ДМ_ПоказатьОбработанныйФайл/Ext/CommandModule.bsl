
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	лДанныеФайла = ДМ_ИнтеграцияУВСSAP.ПолучитьФайлОбработанный( ПараметрКоманды );
	Если лДанныеФайла = Неопределено Тогда
		Возврат;
	КонецЕсли;	
	лТекстовыйДокумент = лДанныеФайла.ФайлОбработанныйПоОбменуСМеркурием;
	лТекстовыйДокумент.Показать( лДанныеФайла.ИмяФайла, лДанныеФайла.ИмяФайла );
КонецПроцедуры

